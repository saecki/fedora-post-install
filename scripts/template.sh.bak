#!/bin/sh

install() {
    echo "No install action defined"
}

update() {
    echo "No update action defined"
}

while getopts "iu" opt; do
    case "$opt" in 
	i ) install;;
	u ) update;;
    esac
done

